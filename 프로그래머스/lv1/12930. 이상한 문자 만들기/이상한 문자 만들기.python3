def solution(s):
    s = s.lower()
    s = list(s)
    cnt = 0

    for i in range(len(s)):
        if s[i] == ' ':
            cnt = 0
        elif cnt % 2 == 0:
            s[i] = s[i].upper()
            cnt += 1
        else:
            cnt += 1
    return ''.join(s)